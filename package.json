{
	"name": "skill-tree",
	"scripts": {
		"serve": "http-server --push-state -o -c-1 -p 3011",
		"s": "npm run serve",
		"deploy": "firebase deploy",
		"lit-check": "npx lit-analyzer . --strict"
	},
	"dependencies": {
	}
}
