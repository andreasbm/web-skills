{
	"name": "skill-tree",
	"type": "module",
	"scripts": {
		"serve": "http-server --push-state -o -c-1 -p 3011 -a localhost",
		"s": "npm run serve",
		"deploy": "firebase deploy",
		"lit-check": "npx lit-analyzer . --strict",
		"generate-blueprint": "node --experimental-modules generate-blueprint.mjs"
	},
	"dependencies": {
	}
}
